#include "sqrt.h"

/**
 * @brief Checks if the given parameters `a` and `b` differ less than
 *        `precision`
 * @param a Value to check for being equal to `b`
 * @param b Value to check for being equal to `a`
 * @param precision If `a` and `b` differ less than this value, they will be
 *                  considered as equal
 * @retval 0 The values `a` and `b` are *NOT* equal
 * @retval 1 The values `a` and `b` are equal (with precision `precision`)
 */
int
is_equal(double a, double b, double precision)
{
	// Return "is a euqal to b with a precision of 'precision'?"
}
